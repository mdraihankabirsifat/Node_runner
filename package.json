{
  "name": "node-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Server-authoritative multiplayer 2D survival game built with JavaScript, Canvas and Socket.IO.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check server/GameRoom.js && node --check server/arena.js && node --check public/js/main.js && node --check public/js/GameClient.js && node --check public/js/Renderer.js && node --check public/js/UI.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.3"
  }
}
